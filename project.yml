name: CopyKit
options:
  bundleIdPrefix: com.hsbc
  
  
targets:

  CopyKit:
    type: tool
    sources: CopyKit
    platform: macOS
    dependencies:
      - framework: Commandant.framework

  SampleApp:
    type: application
    sources: SampleApp
    platform: iOS
    deploymentTarget: "12.0"

  ConvertCommand:
    type: tool
    platform: macOS



    
    
